# Sign-language-detector
A real-time Sign Language Detection system built with Python, TensorFlow, LSTM and OpenCV. The system captures live hand gesture sequences through a webcam, processes them using MediaPipe for landmark detection, and classifies them into text using a trained LSTM deep learning model. Designed to bridge the communication gap for the hearing and speech impaired community.
